spring:
  application:
    name: course-management
  datasource:
    url: jdbc:mysql://localhost:3306/seguimiento?useSSL=false&serverTimezone=UTC&
    username: "gary@gmail.com"
    password: "12345"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true